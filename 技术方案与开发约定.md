# 电商运营助手技术方案与开发约定

## 1. 已确定技术方案

| 层级 | 方案 | 说明 |
|---|---|---|
| 前端 | Vue 3、TypeScript、Vite、Element Plus | 组件化后台工作台，启用严格类型检查 |
| 前端状态与请求 | Pinia、Vue Router、Axios | 分离登录态、页面路由和 API 调用 |
| 后端 | Python 3.12+、FastAPI、Pydantic | OpenAPI 自动生成、结构化校验 |
| 数据访问 | SQLAlchemy 2.x、Alembic | ORM 与版本化数据库迁移 |
| 数据库 | MySQL 8.4、`utf8mb4` | 当前开发库为 `ecommerce_ops` |
| 密码与令牌 | Argon2、JWT | 密码只保存强哈希，令牌短期有效 |
| 文本 AI | Qwen-Max / Qwen-Plus | 通用文本保留 Qwen-Max，严格 JSON Schema 默认使用 Qwen-Plus |
| 图片 AI | `qwen-image-3.0` | 首选商品主图生成与编辑模型 |
| 视频 AI | Wan 系列 | 通过异步任务接入，不阻塞 HTTP 请求 |
| 异步任务 | 首版使用 MySQL 任务表与 Worker | 降低部署依赖，达到容量阈值后可迁移 Redis |
| 素材存储 | 本地目录 / Docker 共享卷 | 数据库存储 Key 与元数据；多节点生产可按统一接口扩展 OSS |
| 测试 | Pytest、API E2E、Ruff、vue-tsc、Vite | 外部 AI/媒体由 Mock 替代，加入依赖审计与安全扫描 |
| 发布 | Docker Compose、Nginx、GitHub Actions | API 固定 8011，Web 默认 8080，不使用 8000 |

### 1.1 启动决策项结论

| 编号 | 最终结论 |
|---|---|
| Q-01 | Vue 3 + FastAPI + SQLAlchemy/Alembic + MySQL 8.4；首版队列使用 MySQL 任务表与独立 Worker |
| Q-02 | 首版单工作区、多店铺，不实现多租户 |
| Q-03 | 短期 Access Token，含过期时间和 JTI；退出时服务端撤销，首版不发放 Refresh Token |
| Q-04 | 运营人员可维护店铺业务信息，不管理用户、系统设置或明文平台凭证 |
| Q-05 | 文本和媒体统一 Provider 接口；提供确定性 Mock 与 DashScope/Qwen 适配器 |
| Q-06 | v0.1 开发使用本地目录，Docker 使用共享卷；对象存储作为多节点扩展项 |
| Q-07 | 只解析配置白名单中的公开 HTTP(S) 商品链接和公开元数据 |
| Q-08 | 禁止负库存；锁定量不得超过总库存；使用事务、行锁与版本号 |
| Q-09 | `CTR=点击/曝光`、`转化率=转化/点击`、`ROI=收入/花费`，保留 6 位，分母为零返回 `null` |
| Q-10 | v0.1 统一 UTF-8 CSV，最大 2MB/1000 数据行；小于原建议值以保证同步预览可控 |
| Q-11 | 诊断使用 `source_review_report_id` 关联来源复盘，报告详情反查下一轮诊断 |
| Q-12 | 等待中可立即取消；执行中取消保证本地不再落库结果，但不承诺上游任务停止 |

### 1.2 v0.1 容量基线

- 首版服务对象为单工作区中小团队，不作大促高并发承诺。
- 所有业务列表必须分页，单页不超过 100；工作台聚合查询数与返回行数无关。
- 单个 CSV 不超过 2MB/1000 数据行，发布机预览门禁为 15 秒内完成。
- Worker 并发可配置为 1～16；任务必须满足幂等创建、互斥领取、锁过期恢复和超时终止。
- 库存以事务和乐观版本防止丢更新；点击记录与有效计数必须在同事务。
- 超出上述范围前，在目标服务器重做容量测试并评估对象存储、外部队列和水平扩展。

## 2. 代码目录约定

```text
backend/app/api/             HTTP 路由与依赖
backend/app/models/          ORM 数据模型
backend/app/services/        业务事务与状态机
backend/app/repositories/    复杂数据查询（按需引入）
backend/app/integrations/    AI、对象存储、平台接口适配器
backend/app/workers/         异步任务执行器
backend/migrations/          Alembic 数据库迁移
backend/tests/               后端自动化测试
frontend/src/api/            API 客户端
frontend/src/stores/         Pinia 状态
frontend/src/router/         路由与访问控制
frontend/src/views/          页面级组件
frontend/src/components/     可复用组件
scripts/                     本地准备与启动脚本
storage/                     本地开发素材（内容不提交）
```

## 3. 数据与事务约定

- 所有日期时间由后端按 UTC 处理，前端按用户时区展示。
- 金额使用 `DECIMAL`，禁止使用浮点数。
- 稳定且需要筛选的数据使用普通列；不稳定的结构化内容使用 JSON 并由 Pydantic Schema 校验。
- 库存更新和库存流水必须处于同一事务，并使用版本号或行锁防止并发丢失。
- 写接口必须校验目标对象的店铺/商品归属，不能只校验用户角色。
- 导入预览阶段不写业务表；确认导入后按行记录成功与失败结果。
- 所有迁移先在测试环境验证；自动生成的迁移必须人工检查后执行。

### 3.1 店铺与平台账号规则

- 店铺平台使用固定枚举；同一平台的 `external_store_id` 非空时必须唯一。
- 店铺负责人可为空；设置负责人时必须关联一个状态为 `active` 的应用用户。
- 低库存口径为：有效商品及有效 SKU 的 `stock_qty - locked_qty <= warning_threshold`。
- 平台账号完整标识仅在数据库内部用于唯一性判断，API 一律返回脱敏标识。
- `auth_meta_json` 持久化白名单为卖家 ID、权限范围、授权过期时间、最近授权时间、错误码，以及授权过程中临时使用的 state 摘要和开始时间。
- 授权 `state` 原文不落库，只保存 SHA-256 摘要；回调成功或失败后立即删除摘要，阻止重放。
- 平台密码、Access Token、Refresh Token、Cookie、Secret 等凭据当前阶段不接收、不落库、不写日志；正式接入平台时须通过密钥服务或字段级加密保存。

### 3.2 商品与平台映射规则

- 商品必须归属于有效店铺，平台字段从店铺自动继承，创建后不允许跨店铺迁移。
- 停用店铺不能新增或重新启用商品；商品删除统一转换为 `inactive`，不物理级联删除业务数据。
- 商品价格和成本使用 `DECIMAL(12,2)`，必须非负；商品链接和图片仅接受 HTTP(S) URL。
- 平台映射归属店铺和商品；同一店铺的“平台 + 平台商品编号 + 平台 SKU 编号”必须唯一。
- 映射更新和删除同时校验 URL 中的商品 ID，跨商品操作统一返回资源不存在。
- 商品列表和详情的最近诊断/复盘时间使用底层记录 `created_at` 最大值实时聚合。

### 3.3 SKU 规则

- SKU 必须归属于 URL 指定商品；跨商品查询或写入按资源不存在处理，避免暴露其他商品数据。
- `sku_code` 在同一商品内按不区分大小写唯一，只允许字母、数字、点、下划线和连字符。
- `spec_json` 使用扁平字符串键值对象，最多 20 项；规格名称最长 50 字符，规格值最长 100 字符。
- SKU 价格和成本使用 `DECIMAL(12,2)` 且非负，成本和平台 SKU 编号允许清空。
- 停用商品或店铺不能新增/启用 SKU；SKU 不提供物理删除接口，停用后保留库存和全部流水。

### 3.4 库存与流水规则

- 每个 SKU 对应且仅对应一条库存记录；新增 SKU 同事务创建零库存，历史 SKU 通过迁移补齐。
- 可用库存口径为 `stock_qty - locked_qty`；有效店铺、商品和 SKU 的可用库存小于等于预警阈值时计为低库存。
- 数量调整分为入库、出库、盘点调整、锁定和解锁；入库/锁定使用正数，出库/解锁使用负数。
- 总库存和锁定量不得为负，锁定量不得超过总库存；停用对象只能减少库存或解锁，不能增加或锁定。
- 每次数量调整在同一事务中锁定库存行、校验 `expected_version`、更新数量和版本，并且只写一条流水。
- 预警阈值和库位配置也校验 `expected_version`；版本冲突返回最新版本，由客户端刷新后重试。
- 流水永久保留调整前后数量、原因、引用来源和操作人，不随 SKU 停用而删除。

### 3.5 库存建议规则

- 首版使用可解释规则引擎，不调用 AI，不直接执行入库或修改任何库存字段。
- 默认规则汇总近 30 天 `outbound` 流水，以日均出库量、14 天覆盖周期和 1.2 安全系数计算目标库存。
- 目标库存至少为预警阈值加一；建议补货量为 `max(0, 目标库存 - 可用库存)`。
- 默认出库事件少于 2 次时标记 `insufficient`，动作设为 `monitor` 并明确提示人工复核。
- 每次生成保存规则版本、参数、汇总信息和 SKU 级输入/结果快照，支持历史核对和规则升级。
- 只有具备 `inventory.write` 权限的用户可生成建议；所有登录用户可查看已有建议。

### 3.6 推广链接、投放建议与实验规则

- 推广目标只接受公开 HTTP(S) URL，拒绝认证信息、localhost、`.local` 和非公网 IP；跳转目标必须来自已持久化且处于启用状态的链接，不能由请求参数临时指定。
- 追踪码使用密码学安全随机数；UTM 仅允许 `source/medium/campaign/term/content` 五类标准字段。
- 点击 IP 不保存原文，仅保存以服务端密钥生成的 HMAC 摘要；疑似机器人和同一链接 10 分钟内重复访问留痕但不计数。
- 投放建议只能引用本商品已审核且文件可用的素材及启用链接，并保存不可变输入快照、原始输出、模型与 Prompt/Schema 版本。
- 建议在 `pending` 状态允许带版本号编辑八类结构化内容；仅允许 `pending → confirmed/rejected`，终态不可撤回且内容锁定，确认/驳回保存操作人、时间、备注和审计记录。
- 实验只能从已确认建议生成；素材、链接需再次校验商品归属和可用状态，预算非负，草稿确认后内容锁定。
- 实验状态为 `draft → confirmed → running → finished`，非终态可取消；所有写操作使用版本号拒绝并发覆盖。
- 投放模块只提供建议和实验计划，不对接真实广告创建、扣费或预算修改接口。

### 3.7 经营指标与复盘规则

- 有效经营记录按商品禁止周期重叠，周期包含起止日；软作废记录永久保留，但不参与指标汇总和复盘生成。
- 曝光、点击、转化、花费和收入必须非负，并满足 `转化数 <= 点击数 <= 曝光数`；关联的方案、素材、推广链接和实验必须属于同一商品。
- CTR 为 `点击数 / 曝光数`，转化率为 `转化数 / 点击数`，ROI 为 `收入 / 花费`；派生指标查询时计算并保留 6 位小数，分母为零时返回 `null`。
- 复盘只读取完整落入所选周期的有效经营记录；生成时冻结逐条记录和汇总结果，后续经营数据变更不反向修改历史快照。
- 复盘报告固定包含周期摘要、核心洞察、问题判断和下一步动作；保存原始模型输出、Provider、模型及 Prompt/Schema 版本。
- 人工编辑使用乐观锁，每次保存新增不可变版本；报告主记录指向最新内容，版本历史可完整回溯。
- 从复盘开启下一轮诊断时写入 `source_review_report_id`；报告详情反查其后续诊断，形成双向可追溯闭环。

### 3.8 导入与演示数据规则

- 首版批量导入统一使用 UTF-8 CSV，单文件最大 2MB、最多 1000 个数据行；类型、大小、编码、表头和空文件在创建批次前拒绝。
- 上传阶段只写 `import_batches` 与 `import_rows` 校验预览，不写业务表；每行保留原始值、标准化值、状态和可定位到列的稳定错误信息。
- 客户端为每次文件选择提供稳定幂等键；相同键重复上传返回原批次，相同批次重复确认返回既有结果，不重复写业务数据。
- 确认时锁定批次，每个有效行使用独立事务保存点；单行失败不回滚其他成功行，批次结束时 `success_rows + failed_rows = total_rows`。
- 商品导入采用同店同名拒绝策略；SKU 库存采用“商品 ID + SKU 编码”更新策略，库存字段表达目标数量，所有数量变化必须生成正式库存流水并引用导入批次。
- 经营数据导入完全复用手工录入校验及指标公式，并拒绝数据库中或同一文件内的有效周期重叠。
- 上传内容解析完成后不保留临时文件；错误行由持久化行结果即时生成 CSV，避免临时文件泄露和清理遗漏。
- 演示数据使用固定 `external_store_id` 作为标识，只允许管理员初始化；重复执行不复制数据，`rebuild=true` 用于将核心演示店铺状态校准到可用基线。

### 3.9 工作台与运行设置规则

- 工作台为有界读模型：卡片和指标使用分组/子查询，最近商品限 12 条，低库存限 10 条，任务与复盘各限 8 条；禁止在结果循环中逐项查询。
- 平台筛选同时约束店铺、商品、库存、任务、素材和复盘口径；卡片统计必须与对应明细页过滤条件保持一致。
- 运行设置只开放已接入实际执行链路的键；每项声明值类型、范围、可选值和 `immediate/new_requests/worker_restart` 生效方式。
- 设置更新仅限管理员，先对单项值及合并后的完整运行配置执行校验，失败时不写库；审计记录仅保留变更键名。
- 敏感值使用由服务端 JWT 密钥派生的 Fernet 密钥加密保存，API 仅返回是否已配置；空值表示保持原密钥，不得将明文写入响应、日志或审计详情。
- 文本模型和竞品解析每次请求合并数据库覆盖；新建生成任务固化当时默认参数；媒体 Provider、存储下载和队列执行参数由 Worker 启动时读取。

## 4. API 约定

- 业务 API 统一前缀：`/api/v1`。
- 资源使用复数名词，动作仅用于生成、确认、取消、重试等非 CRUD 行为。
- 列表接口统一支持 `page`、`page_size`、筛选与排序；默认 `page_size=20`，最大 100。
- 参数校验失败返回 422；未登录 401；无权限 403；资源不存在 404；状态冲突 409。
- 写接口支持请求 ID；生成、导入确认等操作使用幂等键。
- 响应和日志不得包含密码哈希、明文 Token、Cookie、模型 API Key。

建议错误结构：

```json
{
  "code": "PRODUCT_NOT_FOUND",
  "message": "商品不存在或无权访问",
  "request_id": "...",
  "details": null
}
```

## 5. 状态机约定

- 生成任务：`pending → running → succeeded/failed/timeout/cancelled`。
- 失败或超时任务显式重试后回到 `pending`，每次变化写入事件表。
- 创意方案：`draft → selected/archived`，选中方案可归档，不从归档直接恢复为选中。
- 投放建议：`pending → confirmed/rejected`，终态内容锁定，修改时创建新版本。
- 投放实验：`draft → confirmed → running → finished`；除终态外允许取消。
- 素材审核：`pending → approved/rejected`，审核人和审核时间必须同时记录。

## 6. 安全约定

- 根目录 `.env` 只保存在开发机，不提交版本库。
- MySQL 使用项目专用账号，只授予 `ecommerce_ops.*` 权限。
- 应用管理员通过交互式脚本创建，不提供公开默认密码。
- 生产环境必须更换 `JWT_SECRET`，并使用密钥管理服务保存模型和平台凭证。
- 公开链接解析限制协议、域名、响应大小、重定向次数和超时时间，防止 SSRF。
- 平台账号只保存授权状态和允许的安全元数据。

## 7. 本地工作流

首次准备：

```powershell
.\scripts\bootstrap.ps1
.\scripts\create-admin.ps1
```

日常启动两个终端：

```powershell
.\scripts\start-backend.ps1
```

```powershell
.\scripts\start-frontend.ps1
```

提交前验证：

```powershell
& '.\.venv\Scripts\ruff.exe' check backend
& '.\.venv\Scripts\python.exe' -m pytest
Set-Location frontend
npm run build
```
