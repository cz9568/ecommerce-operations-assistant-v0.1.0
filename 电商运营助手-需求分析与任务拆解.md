# 电商运营助手需求分析与任务拆解

> 来源文档：`电商运营助手.md`  
> 文档目的：将原始需求转化为可评审、可排期、可开发、可测试、可验收的工作项。  
> 说明：本文不预设具体前后端技术栈；涉及技术选型的内容先作为“项目启动决策项”。

## 1. 需求结论

### 1.1 产品定位

本项目是面向中小电商团队的“单品运营闭环工作台”。核心价值不是单点生成文案或素材，而是把商品、竞品、诊断、创意方案、生成素材、投放建议、经营数据和复盘结果串联为可追踪、可编辑、可回溯的业务链路。

完整主链路为：

`建店建档 → 录入商品/SKU/库存 → 补充竞品 → 商品诊断 → 主图方案/视频脚本 → 图片/视频生成 → 人工审核素材 → 投放建议 → 人工确认 → 投放实验 → 经营数据回填 → 复盘报告 → 下一轮诊断`

### 1.2 核心领域划分

| 领域 | 核心职责 | 主要数据对象 |
|---|---|---|
| 身份与权限 | 登录、用户、角色、写操作授权 | `users` |
| 店铺与平台 | 店铺档案、平台账号安全元数据 | `stores`、`platform_accounts` |
| 商品与库存 | 商品、平台映射、SKU、库存与流水 | `products`、`product_skus`、`inventory_items`、`inventory_movements` |
| 竞品情报 | 竞品录入、公开链接解析、定时监控 | `competitors`、解析任务、监控及快照 |
| AI 运营 | 诊断、主图方案、视频脚本、投放建议、复盘 | 诊断、方案、建议、报告相关表 |
| 生成任务 | 图片/视频异步生成、重试、取消、超时、事件 | `generation_jobs`、`generation_job_events` |
| 素材管理 | 生成结果、人工审核、版本、标签、评分 | `generated_assets` |
| 推广与实验 | 推广链接、点击、投放建议、实验计划 | 链接、点击、建议、实验相关表 |
| 经营分析 | 指标录入/导入、复盘与下一步动作 | `performance_records`、`review_reports` |
| 工作区能力 | 导入、模板、演示数据、系统配置 | 导入批次、配置类数据（原需求未完整建模） |

### 1.3 角色权限矩阵

建议后端以 RBAC 权限点校验为准，前端仅负责隐藏或禁用入口，不能替代后端鉴权。

| 能力 | 管理员 | 运营人员 | 查看人员 |
|---|:---:|:---:|:---:|
| 查看业务数据和报表 | 是 | 是 | 是 |
| 编辑商品、竞品、方案、素材、经营数据 | 是 | 是 | 否 |
| 创建/重试/取消生成任务 | 是 | 是 | 否 |
| 确认或驳回投放建议 | 是 | 是 | 否 |
| 更新投放实验状态 | 是 | 待确认，建议是 | 否 |
| 管理店铺和平台账号 | 是 | 待确认 | 否 |
| 管理用户与角色 | 是 | 否 | 否 |
| 修改模型、队列、系统参数 | 是 | 否 | 否 |

建议定义的权限点：`user.manage`、`store.manage`、`product.write`、`inventory.write`、`competitor.write`、`ai.generate`、`job.operate`、`asset.review`、`ad.confirm`、`experiment.write`、`performance.write`、`settings.manage`。

### 1.4 非功能要求

原需求主要描述了功能，以下非功能项应纳入交付和验收：

- 安全：密码强哈希；访问令牌过期与撤销；接口统一鉴权；敏感字段禁止写日志；不保存明文平台密码、Token、Cookie。
- 数据隔离：所有商品级数据必须校验其店铺/商品归属，禁止通过替换 ID 越权读取或修改。
- 幂等性：AI 生成、任务重试、导入确认、演示数据初始化、点击记录等接口需要明确重复提交策略。
- 可观测性：请求日志、任务事件、失败原因、重试次数、耗时和关键操作审计应可查询。
- 性能：列表必须分页；大文件导入应限制大小与行数；长耗时生成必须异步执行。
- 一致性：库存调整必须以事务同时更新库存和新增流水；建议确认、实验状态流转应使用条件更新。
- 可维护性：枚举、AI Prompt、模型参数、任务超时和重试次数集中配置。
- 可测试性：外部 AI、对象存储、链接解析均提供可替换的模拟实现，保证本地和 CI 可验收。

## 2. 范围与版本建议

### 2.1 首版必须交付（MVP/P0）

- 登录、三类角色和后端权限校验。
- 店铺、商品、SKU、库存、竞品的基础维护。
- 结构化诊断、主图方案、视频脚本的生成、编辑和保存。
- 图片/视频生成任务状态机、事件、重试、取消和超时；首版允许使用模拟生成器完成稳定演示。
- 素材同步与人工审核。
- 投放建议、人工确认/驳回和实验计划。
- 经营数据手工录入与文件导入、复盘报告。
- 三类导入模板、演示数据和完整演示链路。

### 2.2 次优先级（P1）

- 竞品定时监控与快照对比。
- 推广链接点击统计和基础趋势展示。
- 模型配置、任务队列配置、系统参数管理。
- 更完整的审计日志、监控指标与告警。
- 真实图片/视频模型、对象存储、平台开放接口适配。

### 2.3 建议暂不扩张（P2）

- 真实平台广告计划创建、扣费和预算修改，明确不在范围内。
- 绕过登录、验证码或平台风控的采集。
- 多店铺聚合经营驾驶舱、复杂 BI、自定义报表。
- 多级组织、数据权限到字段级、审批流引擎。
- 实时库存与第三方 ERP 双向同步。

## 3. 开发前必须确认的问题

以下问题不阻止搭建基础工程，但应在对应模块开发前完成产品或技术确认。

| 编号 | 待确认事项 | 建议默认值 | 影响 |
|---|---|---|---|
| Q-01 | 首版前后端、数据库、任务队列技术栈 | 团队熟悉且支持事务/异步任务的方案 | 工程结构、部署与招聘成本 |
| Q-02 | 租户边界 | 首版单工作区，多店铺；暂不做多租户 | 所有表和权限模型 |
| Q-03 | 访问令牌方案 | 短期 Access Token；生产补充刷新/撤销机制 | 登录安全与前端状态保持 |
| Q-04 | 运营人员能否管理店铺/平台账号 | 建议可编辑店铺业务信息，不可改授权信息 | 权限矩阵 |
| Q-05 | AI/图片/视频供应商 | 统一 Provider 接口，首版至少有 Mock | 成本、稳定性、字段设计 |
| Q-06 | 素材文件存储 | 对象存储；开发环境可用本地兼容实现 | 上传、访问 URL、清理策略 |
| Q-07 | 链接解析支持的平台和字段 | 首版限定白名单平台与公开字段 | 合规、解析成功率 |
| Q-08 | 库存是否允许负数 | 建议默认禁止，管理员可配置 | 并发更新与校验 |
| Q-09 | 经营指标口径 | 明确 CTR、转化率、ROI 的分母与四舍五入规则 | 导入、统计、报表一致性 |
| Q-10 | 导入文件格式与限制 | 首版 XLSX/CSV，10 MB、5,000 行 | 性能与错误反馈 |
| Q-11 | 复盘到下一轮诊断如何关联 | 为诊断增加 `source_review_report_id` 或轮次实体 | 闭环可追溯性 |
| Q-12 | 任务取消的语义 | 未执行可立即取消；执行中尽力取消并记录结果 | Worker 与第三方接口 |

## 4. 原需求缺口与建议修订

### 4.1 接口缺口

原文列出的接口偏重创建动作，前端完成页面还需要以下接口。开发时应先补充 OpenAPI 契约，再实现页面：

- SKU：列表、新增、更新、启停用接口。
- 库存：库存列表/概览、库存调整、库存流水分页、低库存列表接口。
- 平台账号：列表、更新、停用/删除接口，以及脱敏授权状态查询。
- 竞品：列表、详情、更新、删除；解析任务和监控快照查询接口。
- 诊断/方案：列表与详情查询；方案状态切换规则和归档接口。
- 生成任务：列表、详情、事件时间线查询接口。
- 素材：列表、详情、单条更新；原 `PATCH .../creative-plans/{id}/assets` 路径无法明确具体素材。
- 推广链接：列表、详情、停用；点击统计查询接口。
- 投放建议/实验：列表、详情查询；实验状态流转动作或带版本号更新。
- 经营数据/复盘：记录列表、详情、更新/删除，复盘报告列表与详情接口。
- 导入中心：上传、校验预览、确认导入、导入批次状态/结果查询接口。
- 设置：模型配置、任务配置、系统参数的查询与更新接口。
- 工作台：店铺卡片、低库存数量、最近任务、最近复盘等聚合查询接口。

### 4.2 数据模型缺口

建议在数据库设计任务中补齐或确认：

- 所有业务表统一主键类型、时区、软删除策略、创建人/更新人字段。
- `stores` 是否需要状态字段；店铺负责人应关联用户还是仅文本。
- `platform_accounts.auth_meta_json` 的允许字段、加密字段和脱敏返回规则。
- `product_diagnoses`、`creative_plans`、`ad_recommendations`、`review_reports` 增加版本/生成状态/生成模型/Prompt 版本，避免编辑后无法追溯原始 AI 输出。
- `generation_jobs` 缺少 `created_at`、`updated_at`、取消时间/取消人、幂等键和进度字段。
- `generated_assets` 缺少 `updated_at`、审核人、审核时间；建议将文件存储 Key 与公开 URL 分开。
- `ad_recommendations` 缺少 `updated_at`；确认记录若需审计，建议独立事件表。
- `performance_records` 应约束周期合法、非负指标、关联对象属于同一商品，并明确派生指标是存储还是查询计算。
- 增加 `import_batches`、`import_rows`，支持预览、部分成功、错误行和结果追踪。
- 增加 `system_settings`/`model_configs`/`queue_configs` 或明确改用环境配置且不在 UI 修改。
- 增加审计日志表，记录用户管理、投放确认、实验状态和系统设置等高风险操作。
- “从复盘回到诊断”的关联字段或运营轮次实体尚未定义。

### 4.3 业务规则缺口

- 方案从 `draft` 到 `selected`/`archived` 的合法流转，以及同一商品、同一方案类型是否只能有一个 `selected`。
- 任务重试后复用原任务还是创建子任务；建议同一任务累加尝试次数并记录每次事件，超过上限禁止重试。
- 任务 `cancelled`、`timeout` 是否允许重试；建议允许显式重试并进入 `pending`。
- 素材审核状态枚举未定义；建议至少包含 `pending`、`approved`、`rejected`。
- 投放建议确认后是否允许再次修改；建议内容锁定，修改需复制为新版本。
- 投放实验各状态合法流转需定义，建议：`draft → confirmed → running → finished`，除 `finished/cancelled` 外可转 `cancelled`。
- 点击统计防刷、IP 保存周期及隐私策略尚未定义。
- 演示数据接口重复调用是复用、覆盖还是新建；建议使用固定标识保证幂等。
- 删除策略未定义；有关联数据时建议停用或软删除，不做级联物理删除。

## 5. 交付物清单

最终交付应至少包含：

1. 可运行的前端工程和生产构建产物。
2. 可运行的后端 API 与异步 Worker。
3. 数据库迁移/初始化脚本及种子管理员创建方式。
4. OpenAPI 接口文档、错误码和状态机说明。
5. 商品、SKU 库存、经营数据导入模板。
6. 演示数据初始化及一条完整的单品闭环演示脚本。
7. 环境变量模板、部署说明、备份恢复说明。
8. 自动化测试与测试报告。
9. 合规与安全检查清单。

## 6. 任务拆解（WBS）

### 6.1 M0：需求冻结与工程基础

#### M0-01 需求澄清和范围基线（P0）

- 输出：待确认问题结论、MVP 范围、术语表、角色权限矩阵。
- 子任务：
  - 确认 Q-01～Q-12。
  - 统一“方案、任务、素材、建议、实验、复盘、运营轮次”等术语。
  - 明确删除、归档、停用的使用边界。
  - 为验收项指定负责人和证据形式。
- 完成标准：产品、开发、测试对同一份范围基线完成评审；无阻塞性歧义。
- 依赖：无。

#### M0-02 技术方案与架构设计（P0）

- 输出：架构图、模块边界、部署拓扑、外部依赖清单和 ADR。
- 子任务：
  - 选择前端、后端、数据库、缓存/队列、Worker、文件存储方案。
  - 设计同步 API、异步任务、AI Provider、对象存储的调用边界。
  - 设计开发/测试/生产环境配置和密钥管理。
  - 明确 Mock Provider 与真实 Provider 的切换方式。
- 完成标准：关键选型有理由、替代方案和风险；本地环境可在无真实 AI 密钥时运行。
- 依赖：M0-01。

#### M0-03 工程脚手架与质量门禁（P0）

- 输出：前端、后端、Worker 工程；统一命令；CI 配置。
- 子任务：
  - 初始化目录、依赖管理、环境变量样例和配置加载。
  - 配置格式化、静态检查、类型检查、单元测试和构建命令。
  - 配置数据库迁移、测试数据库和种子数据机制。
  - 建立分支/提交规范及 CI 最低门禁。
- 完成标准：新环境按 README 可启动；CI 能执行检查、测试和构建。
- 依赖：M0-02。

#### M0-04 API 与错误规范（P0）

- 输出：OpenAPI 初稿、分页/筛选/排序、错误码、时间与金额规范。
- 子任务：
  - 统一响应、分页元数据和参数校验错误格式。
  - 明确 ID、金额精度、日期周期、时区和枚举序列化。
  - 统一 401、403、404、409、422、429、500 的使用。
  - 补齐第 4.1 节所列查询和维护接口。
- 完成标准：前后端可根据契约并行开发；核心链路不存在无接口可用的页面。
- 依赖：M0-01、M0-02。

#### M0-05 数据库详细设计（P0）

- 输出：ER 图、字段字典、约束/索引、迁移脚本。
- 子任务：
  - 校正原需求 22 类表的字段、类型、默认值和外键。
  - 补充导入批次、审计日志、设置及复盘闭环关联模型。
  - 为商品归属、任务扫描、低库存、监控到期、周期查询设计索引。
  - 规定软删除、唯一约束、JSON Schema 和级联策略。
- 完成标准：迁移可全新执行和回滚；测试数据可插入；关键约束有自动化测试。
- 依赖：M0-01、M0-02。

### 6.2 M1：认证、用户与基础框架

#### M1-01 后端认证（P0）

- 输出：登录、当前用户、退出/令牌失效能力。
- 子任务：密码强哈希、登录状态校验、禁用用户拦截、登录时间更新、统一 401。
- 完成标准：正确密码可登录；错误密码、禁用用户、过期令牌均被拒绝；响应不泄露密码哈希。
- 依赖：M0-03、M0-05。

#### M1-02 RBAC 权限中间件（P0）

- 输出：权限点定义、路由授权、对象归属校验辅助方法。
- 子任务：覆盖所有写接口；单独保护用户管理、投放确认、实验状态和系统设置；增加越权测试。
- 完成标准：三类角色与权限矩阵一致；直接调用隐藏按钮对应接口同样会被拒绝。
- 依赖：M1-01。

#### M1-03 用户管理（P0）

- 输出：用户列表、创建、编辑角色、启停用。
- 子任务：用户名唯一、初始密码策略、禁止非管理员管理用户、防止最后一个管理员被停用。
- 完成标准：管理员可维护用户；其他角色返回 403；用户不可查看密码字段。
- 依赖：M1-02。

#### M1-04 前端认证与应用壳（P0）

- 输出：登录页、路由守卫、登录态恢复、退出、403/404/错误页、主导航。
- 子任务：处理令牌失效、刷新后用户恢复、按权限展示入口、统一请求错误提示。
- 完成标准：未登录不能进入业务页；角色切换后菜单和操作按钮符合权限。
- 依赖：M1-01、M1-02。

#### M1-05 通用前端能力（P0）

- 输出：表格分页、筛选、表单校验、确认弹窗、空状态、加载状态、错误反馈组件。
- 完成标准：后续模块复用统一交互；危险操作均有二次确认和明确结果。
- 依赖：M1-04。

### 6.3 M2：店铺、商品、SKU 与库存

#### M2-01 店铺后端（P0）

- 输出：店铺列表、创建、详情、更新及聚合数据接口。
- 子任务：平台枚举、店铺状态、负责人规则、商品数和低库存数聚合。
- 完成标准：列表可分页筛选；详情统计与底层数据一致。
- 依赖：M0-05、M1-02。

#### M2-02 平台账号占位（P0）

- 输出：账号维护、授权开始、授权回调占位、脱敏查询。
- 子任务：定义 `auth_meta_json` 白名单；敏感值加密或不落库；日志脱敏。
- 完成标准：库表、响应和日志均无明文密码/Token/Cookie；授权状态可展示。
- 依赖：M2-01。

#### M2-03 商品与平台映射后端（P0）

- 输出：商品 CRUD/状态切换、列表筛选、平台商品映射。
- 子任务：价格/成本校验、商品归属校验、映射唯一约束、最近诊断/复盘时间聚合。
- 完成标准：商品可按店铺、平台、状态查询；跨商品映射操作被拒绝。
- 依赖：M2-01。

#### M2-04 SKU 管理（P0）

- 输出：SKU 列表、新增、编辑、启停用。
- 子任务：商品内 SKU 编码唯一；规格 JSON 校验；价格和成本精度校验。
- 完成标准：SKU 可维护且历史库存流水不因停用丢失。
- 依赖：M2-03。

#### M2-05 库存与流水（P0）

- 输出：库存查询、库存调整、流水分页、低库存查询。
- 子任务：事务锁/乐观锁、调整前后数量记录、负库存规则、引用来源、预警阈值。
- 完成标准：并发调整不丢失更新；每次变更恰有一条流水；低库存判断正确。
- 依赖：M2-04。

#### M2-06 库存建议（P1）

- 输出：店铺维度库存建议生成和展示。
- 子任务：定义规则输入、建议结果结构、数据不足提示；首版可用规则引擎而非 AI。
- 完成标准：建议可解释并能定位到 SKU；不会直接修改库存。
- 依赖：M2-05。

#### M2-07 店铺页面（P0）

- 输出：店铺列表/详情/编辑、库存概览、账号占位和库存建议界面。
- 完成标准：页面字段与权限矩阵一致；统计、流水和预警可追溯到具体商品/SKU。
- 依赖：M2-01、M2-02、M2-05。

#### M2-08 商品列表与详情骨架（P0）

- 输出：商品列表、创建/编辑，以及九个标签页的详情框架。
- 子任务：保留当前商品上下文；标签 URL 可直接访问；无权限时只读；未保存离开提示。
- 完成标准：能从店铺/商品列表进入同一商品的各标签；不存在串商品数据。
- 依赖：M2-03、M1-05。

#### M2-09 SKU 与库存前端（P0）

- 输出：SKU 表格与表单、库存调整、库存流水、预警提示。
- 完成标准：调整结果即时刷新；失败不产生假成功；查看人员无法提交。
- 依赖：M2-04、M2-05、M2-08。

### 6.4 M3：竞品与公开链接

#### M3-01 竞品维护（P0）

- 输出：竞品列表、新增、编辑、删除/停用。
- 子任务：商品归属、URL 校验、价格与公开字段维护、删除策略。
- 完成标准：竞品只能挂在指定商品下；列表和详情正确展示。
- 依赖：M2-03。

#### M3-02 公开链接解析任务（P0）

- 输出：创建、执行、查询解析任务及回填竞品字段。
- 子任务：域名白名单、请求超时/大小限制、重试、错误分类、结果确认后回填。
- 完成标准：仅解析公开可访问信息；验证码/登录场景明确失败；不绕过平台控制。
- 依赖：M3-01、M6-01 的部分任务基础能力可复用。

#### M3-03 竞品监控与快照（P1）

- 输出：监控配置、到期任务执行、快照列表与变化展示。
- 子任务：合法间隔范围、重复执行锁、失败退避、下一次时间计算、停用。
- 完成标准：到期监控不会并发重复运行；变化前后值可追踪。
- 依赖：M3-02、M6-02。

#### M3-04 竞品前端（P0）

- 输出：竞品维护、URL 解析进度/错误、监控配置和快照展示。
- 完成标准：用户能区分手工值与解析值，并决定是否采用解析结果。
- 依赖：M3-01、M3-02；监控展示依赖 M3-03。

### 6.5 M4：AI 基础与商品诊断

#### M4-01 AI Provider 抽象（P0）

- 输出：统一文本生成接口、Mock Provider、超时/重试/限流和用量日志。
- 子任务：模型配置、结构化输出 Schema 校验、敏感配置隔离、错误映射、Prompt 版本记录。
- 完成标准：无外部密钥时 Mock 可跑通；输出不符合 Schema 时可纠正或明确失败。
- 依赖：M0-02、M0-03。

#### M4-02 Prompt 与输出结构设计（P0）

- 输出：诊断、主图、视频、投放、复盘五类 Prompt 模板和 JSON Schema。
- 子任务：输入字段裁剪、缺失数据提示、版本号、样例、注入防护与最大长度。
- 完成标准：每类输出覆盖原需求的全部必填结构；可反序列化并编辑保存。
- 依赖：M4-01、M0-01。

#### M4-03 商品诊断后端（P0）

- 输出：生成、列表、详情、编辑保存商品诊断。
- 子任务：聚合商品/竞品输入、结构化校验、保存原始输出与编辑后字段、并发更新控制。
- 完成标准：诊断包含七类必需内容；生成失败不产生伪成功记录；编辑结果可重新打开。
- 依赖：M3-01、M4-02。

#### M4-04 商品诊断前端（P0）

- 输出：生成确认、结构化展示、编辑保存、历史版本选择。
- 完成标准：加载/生成/失败/空数据状态明确；用户可编辑每个结构字段。
- 依赖：M4-03、M2-08。

### 6.6 M5：创意方案

#### M5-01 主图方案后端（P0）

- 输出：生成至少 3 个方向、查询、编辑和状态流转。
- 子任务：五类必需字段、方案版本、同类唯一选中规则、归档校验。
- 完成标准：每次有效生成至少 3 个完整方向；状态流转符合规则。
- 依赖：M4-02、M4-03。

#### M5-02 视频脚本后端（P0）

- 输出：生成至少 3 条脚本、查询、编辑和状态流转。
- 子任务：钩子、分镜、口播、转化引导的结构化保存和编辑。
- 完成标准：每次有效生成至少 3 条完整脚本；历史版本可追溯。
- 依赖：M4-02、M4-03。

#### M5-03 主图方案前端（P0）

- 输出：方案卡片、结构化编辑、草稿/选中/归档、发起图片生成。
- 完成标准：状态反馈与后端一致；归档方案不可误提交生成。
- 依赖：M5-01、M2-08；生成按钮最终依赖 M6-03。

#### M5-04 视频脚本前端（P0）

- 输出：脚本卡片/分镜编辑、状态操作、发起视频生成。
- 完成标准：脚本字段完整可编辑；生成操作引用正确方案版本。
- 依赖：M5-02、M2-08；生成按钮最终依赖 M6-03。

### 6.7 M6：异步生成任务

#### M6-01 任务状态机与仓储（P0）

- 输出：任务创建、查询、事件时间线及状态转换服务。
- 子任务：定义六种状态合法转换、幂等键、最大尝试次数、进度、错误分类、乐观锁。
- 完成标准：非法状态跳转被拒绝；每次状态变化都有事件；重复请求不重复创建任务。
- 依赖：M0-05。

#### M6-02 Worker 与队列（P0）

- 输出：任务领取、心跳/锁、执行、成功/失败、退避重试和超时扫描。
- 子任务：防重复消费、锁过期恢复、优雅停机、并发限制、死信/最终失败处理。
- 完成标准：Worker 异常重启后任务可恢复；同一任务结果不会重复落库。
- 依赖：M6-01、M0-02。

#### M6-03 图片/视频生成适配器（P0）

- 输出：Mock 生成器、真实 Provider 扩展接口、创建生成任务 API。
- 子任务：输入快照、尺寸/时长参数、外部任务 ID、轮询/回调策略、生成结果校验。
- 完成标准：图片和视频任务均能从 `pending` 走到终态；普通 HTTP 请求不等待生成完成。
- 依赖：M5-01、M5-02、M6-02。

#### M6-04 重试、取消和超时（P0）

- 输出：重试/取消 API、超时扫描入口、权限与审计。
- 子任务：限制可操作状态、取消竞争处理、超上限提示、记录操作人和事件。
- 完成标准：成功任务不可取消；运行中取消语义明确；超时任务被可靠标记。
- 依赖：M6-02。

#### M6-05 任务页（P0）

- 输出：任务列表、状态筛选、自动刷新、详情事件线、重试/取消操作。
- 子任务：轮询退避、页面离开停止刷新、终态停止轮询、错误详情展示。
- 完成标准：任务状态和事件无需手动刷新即可更新；按钮仅在合法状态和权限下显示。
- 依赖：M6-01、M6-04、M2-08。

### 6.8 M7：素材库

#### M7-01 文件与素材落库（P0）

- 输出：文件存储接口、生成结果入库、素材同步、素材查询。
- 子任务：存储 Key、MIME/大小校验、访问 URL、重复同步幂等、失效文件处理。
- 完成标准：成功任务生成对应素材；重复同步不重复落库；文件不可执行上传。
- 依赖：M6-03。

#### M7-02 素材审核与版本（P0）

- 输出：审核状态、版本、场景、评分、标签、备注更新 API。
- 子任务：定义审核枚举、评分范围、版本生成规则、审核人/时间、并发编辑保护。
- 完成标准：字段校验正确；审核操作有审计；素材始终归属于同一商品和方案。
- 依赖：M7-01、M1-02。

#### M7-03 素材库前端（P0）

- 输出：图片/视频网格、预览、筛选、审核、评分、标签、备注和版本展示。
- 完成标准：不同媒体可正常预览；修改成功后数据一致；查看人员只读。
- 依赖：M7-02、M2-08。

### 6.9 M8：推广链接、投放建议与实验

#### M8-01 推广链接建议与创建（P0）

- 输出：链接建议、链接 CRUD/停用、唯一追踪码和 UTM 校验。
- 完成标准：目标 URL 合法；追踪码不可猜测性满足约定；停用链接不再正常跳转。
- 依赖：M2-03、M4-02。

#### M8-02 跳转与点击统计（P0）

- 输出：追踪跳转、点击落库、聚合统计查询。
- 子任务：开放重定向防护、User-Agent 长度、IP 隐私策略、机器人/重复点击基础规则、异步计数一致性。
- 完成标准：合法链接跳转并记录；非法目标与停用链接被拦截；统计可按时间查询。
- 依赖：M8-01。

#### M8-03 投放建议生成（P0）

- 输出：建议生成、列表、详情及八类结构化内容。
- 子任务：选择已审核素材和链接作为输入；保存模型/Prompt 版本；数据不足提示。
- 完成标准：输出覆盖策略摘要、目标、人群、预算、素材测试、出价、风控、下一步动作。
- 依赖：M4-02、M7-02、M8-01。

#### M8-04 投放建议人工确认（P0）

- 输出：确认/驳回、备注、操作人和时间、审计记录。
- 子任务：权限校验、重复确认冲突、确认后内容锁定/新版本规则。
- 完成标准：只有授权角色可操作；状态与审计信息一致；系统不执行真实投放。
- 依赖：M8-03、M1-02。

#### M8-05 投放实验计划（P0）

- 输出：从已确认建议生成实验、编辑及状态流转。
- 子任务：素材/链接同商品校验、预算非负、成功指标、假设、合法状态机、并发更新。
- 完成标准：未确认建议不能生成正式实验；非法状态跳转返回 409/422。
- 依赖：M8-04。

#### M8-06 链接与投放前端（P0）

- 输出：链接列表/统计、建议展示与确认、实验计划编辑与状态更新。
- 完成标准：高风险操作二次确认；确认/驳回记录清晰；不出现“直接投放”误导文案。
- 依赖：M8-02、M8-04、M8-05、M2-08。

### 6.10 M9：经营数据与复盘

#### M9-01 经营数据维护（P0）

- 输出：记录新增、查询、更新、删除/作废。
- 子任务：周期不重叠规则、非负指标、对象同商品校验、CTR/转化率/ROI 口径与精度。
- 完成标准：非法周期和指标被拒绝；派生值计算一致；关联素材/链接/实验可追溯。
- 依赖：M8-05。

#### M9-02 复盘报告生成（P0）

- 输出：报告生成、列表、详情、编辑保存和历史版本。
- 子任务：聚合周期经营数据；输出周期摘要、核心洞察、问题判断、下一步动作；记录输入快照。
- 完成标准：报告只使用指定商品和周期数据；四类内容完整；无数据时给出明确提示。
- 依赖：M4-02、M9-01。

#### M9-03 复盘闭环（P0）

- 输出：从复盘发起下一轮诊断并保留关联。
- 子任务：定义轮次或 `source_review_report_id`；将下一步动作带入新诊断上下文；展示前后轮关联。
- 完成标准：可从任一复盘定位其后续诊断，并从诊断返回来源复盘。
- 依赖：M4-03、M9-02、M0-05。

#### M9-04 复盘前端（P0）

- 输出：经营数据表单/列表、导入入口、复盘生成/编辑、下一步动作和开启新一轮按钮。
- 完成标准：周期指标可核对；报告可编辑保存；闭环跳转携带正确商品与来源。
- 依赖：M9-01、M9-02、M9-03；导入依赖 M10-03。

### 6.11 M10：导入中心、模板与演示数据

#### M10-01 导入框架（P0）

- 输出：上传、解析、字段校验、预览、确认、批次结果与错误行下载。
- 子任务：文件格式/大小/行数限制、表头映射、逐行校验、批次状态、幂等确认、临时文件清理。
- 完成标准：预览不写业务表；确认后支持部分成功；成功/失败数之和等于总行数。
- 依赖：M0-05、M6-01 的状态建模思路。

#### M10-02 商品与 SKU 库存导入（P0）

- 输出：两类模板、字段说明、预览与确认处理器。
- 子任务：店铺/商品定位、重复键策略、SKU 创建与库存调整事务、错误原因定位到行和列。
- 完成标准：合法行正确导入；非法行不污染数据；库存导入产生合规流水。
- 依赖：M2-03、M2-05、M10-01。

#### M10-03 经营数据导入（P0）

- 输出：经营数据模板、预览与确认处理器。
- 子任务：日期/数字格式、关联对象校验、指标口径、重复周期策略。
- 完成标准：导入结果与手工录入校验一致；错误行可下载并修正重传。
- 依赖：M9-01、M10-01。

#### M10-04 导入中心前端（P0）

- 输出：模板下载、文件上传、字段说明、预览、错误行、确认和结果页。
- 完成标准：用户确认前能看到影响范围；部分成功结果清楚区分成功与失败。
- 依赖：M10-01～M10-03。

#### M10-05 演示数据（P0）

- 输出：幂等的演示数据初始化接口和可辨识的完整样例。
- 子任务：覆盖原需求列出的全部对象；建立合法关联；准备不同任务/素材/实验状态；提供清理或重建策略。
- 完成标准：新库一次调用后可直接演示完整闭环；重复调用不会无限复制数据。
- 依赖：M2～M9 的核心表和服务。

### 6.12 M11：工作台与设置

#### M11-01 工作台聚合接口（P0）

- 输出：店铺卡片、平台筛选、商品池、低库存、最近任务/复盘等聚合接口。
- 完成标准：统计口径与各明细页一致；查询有索引且不产生明显 N+1。
- 依赖：M2、M6、M9。

#### M11-02 首页工作台（P0）

- 输出：需求所列入口、卡片、筛选、预警与快捷创建。
- 完成标准：所有入口可达；权限不足的快捷操作不可执行；空环境可引导演示数据初始化。
- 依赖：M11-01、M10-05。

#### M11-03 设置后端（P1）

- 输出：模型配置、系统参数、队列参数查询与更新。
- 子任务：敏感值只写不读/脱敏、配置校验、热更新或重启提示、变更审计。
- 完成标准：仅管理员可访问；响应不返回密钥明文；错误配置不能生效。
- 依赖：M1-02、M4-01、M6-02。

#### M11-04 设置前端（P1）

- 输出：模型、用户、系统、任务配置页面。
- 完成标准：敏感值不回显；保存结果和生效方式明确；非管理员无入口且接口返回 403。
- 依赖：M1-03、M11-03。

### 6.13 M12：测试、安全、文档与发布

#### M12-01 自动化单元与集成测试（P0）

- 输出：核心服务、权限、状态机、导入、库存事务、AI Schema 的测试集。
- 完成标准：关键业务分支和失败分支有测试；测试可重复运行且不依赖真实付费服务。
- 依赖：随各模块持续实施，发布前汇总。

#### M12-02 端到端测试（P0）

- 输出：三类角色权限用例和完整单品闭环 E2E。
- 覆盖：登录 → 建店 → 商品/SKU/库存 → 竞品 → 诊断 → 方案 → 任务 → 素材审核 → 投放建议确认 → 实验 → 经营数据 → 复盘 → 下一轮诊断。
- 完成标准：在全新测试环境可稳定通过；失败时保留可诊断证据。
- 依赖：全部 P0 业务模块。

#### M12-03 安全与合规检查（P0）

- 输出：鉴权、越权、输入、文件、URL、敏感信息、依赖漏洞和合规边界检查报告。
- 完成标准：无高危未处理问题；数据库、响应、日志均不出现禁止保存的明文凭证。
- 依赖：全部 P0 业务模块。

#### M12-04 性能与可靠性验证（P0）

- 输出：列表查询、导入、点击、任务并发和超时恢复测试报告。
- 完成标准：达到 M0-01 确定的容量基线；不存在任务重复执行、库存丢更新或导入失控。
- 依赖：M6、M10、M11。

#### M12-05 文档与演示脚本（P0）

- 输出：README、部署/升级/回滚、配置、API、模板说明、管理员手册、演示讲稿。
- 完成标准：未参与开发的人员可按文档启动系统并完成验收链路。
- 依赖：功能与部署方案稳定。

#### M12-06 发布与验收（P0）

- 输出：发布包、数据库迁移、版本说明、已知问题、验收记录和回滚方案。
- 完成标准：原需求 10 条验收标准全部有通过证据；P0 缺陷关闭或获得书面豁免。
- 依赖：M12-01～M12-05。

## 7. 推荐实施顺序与里程碑

| 里程碑 | 建议内容 | 可演示结果 | 主要依赖 |
|---|---|---|---|
| R0 基础就绪 | M0、M1 | 登录、权限、工程和数据迁移可运行 | 无 |
| R1 商品底座 | M2、M3-01/02 | 建店、商品、SKU、库存、竞品可维护 | R0 |
| R2 AI 策划 | M4、M5 | 可生成并编辑诊断、主图方案、视频脚本 | R1 |
| R3 素材生产 | M6、M7 | 异步生成、任务事件、重试取消、素材审核 | R2 |
| R4 投放闭环 | M8、M9 | 链接、建议确认、实验、数据和复盘闭环 | R3 |
| R5 可交付版本 | M10、M11、M12 | 导入、演示数据、工作台、全链路验收 | R4 |

并行建议：

- M0-04 API 契约与 M0-05 数据库设计可在架构定稿后并行。
- 前端应用壳可与认证后端并行，但联调前必须冻结登录响应和权限字段。
- 主图与视频方案可并行；图片与视频生成适配器也可在统一任务状态机后并行。
- 导入框架可提前建设，但各类型处理器需等待对应领域校验规则稳定。
- 自动化测试、安全检查和文档应伴随模块持续完成，不应全部留到发布阶段。

## 8. 关键依赖关系

```text
需求/架构/契约/数据模型
        ↓
认证与 RBAC
        ↓
店铺 → 商品 → SKU/库存
        ↓       ↓
      竞品    导入处理器
        ↓
AI Provider → 诊断 → 主图/视频方案
                         ↓
                 异步任务 → 素材审核
                              ↓
                    推广链接/投放建议
                              ↓
                         投放实验
                              ↓
                       经营数据 → 复盘
                                      ↓
                                  下一轮诊断
```

## 9. 验收用例映射

| 原验收目标 | 对应任务 | 最低验收证据 |
|---|---|---|
| 登录并按角色显示权限 | M1-01～M1-04、M12-02 | 三类账号的 UI 与 API 权限测试 |
| 店铺、商品、SKU、库存和流水 | M2-01～M2-09 | 创建记录、调整库存及流水截图/API 结果 |
| 竞品和公开链接解析 | M3-01～M3-04 | 成功和合规失败各一例 |
| 诊断、主图方案、视频脚本 | M4、M5 | 结构化输出、编辑保存和历史记录 |
| 图片/视频任务及重试取消 | M6 | 六种状态和事件时间线测试证据 |
| 素材审核、版本、评分和标签 | M7 | 图片/视频素材各一条完整维护记录 |
| 推广链接建议与点击 | M8-01、M8-02 | 跳转、点击明细和聚合统计一致 |
| 投放建议确认/驳回 | M8-03、M8-04 | 权限、状态、操作人与备注记录 |
| 经营数据录入/导入和复盘 | M9、M10-03 | 手工与导入各一例，生成并编辑报告 |
| 演示数据跑通完整闭环 | M10-05、M12-02/05 | 全新环境演示录像或验收记录 |

## 10. 测试重点清单

### 10.1 权限与安全

- 未登录访问每类业务接口返回 401。
- 查看人员调用所有写接口返回 403。
- 运营人员不能管理用户或系统配置。
- 替换 `product_id`、`store_id`、`job_id` 等不能越权操作其他对象。
- 登录、平台授权元数据、模型密钥和日志中无明文敏感凭证。
- 推广跳转不能成为任意 URL 开放重定向。

### 10.2 状态机与并发

- 方案、任务、建议、实验的每个合法和非法状态转换。
- 两个 Worker 同时领取同一任务时只有一个获得执行权。
- 任务成功、取消、超时同时发生时最终状态唯一且事件可解释。
- 两个请求同时调整同一 SKU 库存时数量与流水保持一致。
- 同一导入批次重复确认、演示数据接口重复调用不会重复写入。

### 10.3 AI 与外部依赖

- AI 返回合法 JSON、缺字段、错误类型、空内容、超时、限流时的处理。
- 图片/视频 Provider 同步失败、异步失败、回调重复、结果 URL 失效时的处理。
- 链接解析遇到非白名单域名、重定向、超大响应、登录页、验证码和超时的处理。
- Mock 模式可以稳定产生可验收的结构化内容和素材。

### 10.4 导入与数据质量

- 空文件、错误模板、重复表头、缺列、多列、超限、乱码和公式单元格。
- 一批中同时存在成功行和失败行，统计、事务边界与错误下载正确。
- 商品/SKU 重复键策略一致；库存导入一定产生流水。
- 经营数据日期、金额、比率和关联 ID 的错误能定位到具体行列。

## 11. 项目风险与应对

| 风险 | 等级 | 应对措施 |
|---|:---:|---|
| 首版范围过大导致每个模块只完成表面页面 | 高 | 以完整单品闭环为验收主轴，P0 先形成纵向可运行切片 |
| 真实图片/视频模型成本高、耗时长或不稳定 | 高 | Provider 抽象 + Mock 演示 + 限流/预算/超时配置 |
| 异步任务重复执行或永不结束 | 高 | 幂等键、领取锁、心跳、超时扫描、事件表和恢复测试 |
| 权限只在前端控制造成越权 | 高 | 后端权限点和对象归属校验，增加直接 API 越权用例 |
| JSON 字段大量使用导致查询和演进困难 | 中 | 稳定且需筛选字段结构化；JSON 配 Schema 与版本 |
| 导入部分成功语义不清导致数据污染 | 高 | 预览不落库，逐行事务或明确批次事务，结果可追踪 |
| 指标口径不一致使复盘结果不可信 | 高 | 开发前冻结公式、周期、精度，并用固定样例验算 |
| 公开链接解析触及平台规则 | 高 | 白名单、公开字段、低频、用户触发、完整失败提示，不绕过控制 |
| 复盘与下一轮诊断没有数据关联 | 中 | 增加来源报告/运营轮次模型并作为 P0 完成 |
| 演示数据依赖真实外部服务导致验收不稳定 | 中 | 演示默认使用确定性的 Mock Provider |

## 12. Definition of Done（统一完成标准）

每个功能任务只有同时满足以下条件才视为完成：

- 需求和接口契约已更新，状态、权限、校验和错误行为明确。
- 数据库迁移可在全新环境执行，必要的索引和约束已建立。
- 后端实现鉴权、对象归属校验、参数校验和错误处理。
- 前端包含加载、空数据、错误、无权限和成功反馈状态。
- 关键正常路径、异常路径和权限路径有自动化测试。
- 日志不含敏感数据，关键操作有审计或任务事件。
- 相关 README、OpenAPI 和操作说明同步更新。
- 可使用演示数据独立验证，并通过对应验收用例。

## 13. 首个可运行纵向切片建议

为了尽早验证架构，第一条纵向切片建议只做一个商品的最短闭环：

1. 管理员登录并创建一个店铺。
2. 创建商品、一个 SKU，并执行一次库存入库。
3. 手工录入一个竞品。
4. 使用 Mock AI 生成并编辑商品诊断。
5. 生成 3 个主图方案并选择其中一个。
6. 创建 Mock 图片任务，展示事件并产出一张占位素材。
7. 审核通过素材，生成并确认投放建议。
8. 创建实验，录入一条经营数据，生成复盘报告。
9. 从复盘创建下一轮诊断并验证关联。

这条切片通过后，再补齐视频、批量导入、竞品监控、真实 Provider 和设置等横向能力，可显著降低最后阶段才发现模块无法串联的风险。

## 14. 补充任务（工程治理与部署）

以下任务是在工程初始化和认证模块实施后补充，任务状态以 `项目进度.md` 为准。

### M0-06 通用后端基础设施（P0）

- 输出：统一错误响应、Request ID、结构化日志、分页模型、审计辅助服务、运行配置校验。
- 子任务：
  - 为业务错误、参数错误、HTTP 错误和未知异常提供一致响应结构。
  - 接受或生成 `X-Request-ID`，在响应、日志和审计记录中关联。
  - 日志禁止记录密码、Token、Cookie、API Key 和完整请求体。
  - 提供通用分页响应和最大分页限制。
  - 启动时校验数据库、JWT、AI 和生产调试配置。
- 完成标准：错误响应包含可追踪请求 ID；日志为结构化 JSON；相关测试通过。
- 依赖：M0-03、M0-04。

### M4-05 AI 调用治理（P0）

- 输出：调用追踪、用量与成本记录、限流、内容安全和降级策略。
- 子任务：
  - 记录模型、Prompt 版本、耗时、Token/生成张数和成功状态，不保存密钥。
  - 为文本、图片和视频分别设置并发、超时、重试和预算上限。
  - 区分参数错误、内容安全拦截、限流、供应商故障和结果解析错误。
  - 支持 Mock Provider 和真实 Provider 切换。
- 完成标准：外部模型故障不会阻塞普通接口；每次调用可追踪且成本可统计。
- 依赖：M4-01、M4-02。

### M6-06 Worker 运行与恢复机制（P0）

- 输出：独立 Worker 进程、数据库任务领取锁、心跳、优雅停机和恢复工具。
- 子任务：
  - 使用 MySQL `FOR UPDATE SKIP LOCKED` 或等价方案防止重复领取。
  - 定期续约执行锁并恢复锁过期任务。
  - 进程退出时停止领取新任务，允许当前任务安全结束或回队。
  - 提供 Worker 健康检查和积压任务指标。
- 完成标准：多 Worker 并发不重复产出；异常终止后任务能够恢复。
- 依赖：M6-01、M6-02。

### M7-04 素材生命周期管理（P0）

- 输出：供应商临时文件转存、失效检测、孤儿文件清理和删除审计。
- 子任务：生成结果完成后及时下载到本地存储或 OSS；数据库保存稳定存储 Key。
- 完成标准：供应商临时 URL 过期后素材仍可访问；清理操作不会删除被引用文件。
- 依赖：M7-01。

### M12-07 容器化部署与反向代理（P0）

- 输出：前端、后端、Worker Dockerfile，Compose 编排，Nginx/HTTPS 配置和健康检查。
- 完成标准：在全新服务器按文档可一键部署；应用进程不以 root 用户运行。
- 依赖：核心 P0 模块稳定。

### M12-08 数据备份、恢复与迁移发布（P0）

- 输出：MySQL 备份/恢复脚本、迁移前检查、发布顺序和回滚手册。
- 完成标准：测试环境完成一次备份恢复演练；非事务性 MySQL DDL 失败有处理方案。
- 依赖：M0-05、M12-07。

### M12-09 CI/CD（P1）

- 输出：提交时静态检查、后端测试、前端构建、迁移检查和镜像构建流水线。
- 完成标准：检查失败阻止合并或发布；构建产物具备明确版本号。
- 依赖：M0-03、M12-07。
