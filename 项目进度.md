# 电商运营助手项目进度

> 最后更新：2026-09-18
> 需求与任务基线：`电商运营助手-需求分析与任务拆解.md`  
> 状态定义：✅ 已完成；🟡 部分完成/持续进行；⬜ 未开始；⛔ 阻塞。

## 1. 当前概况

| 项目 | 当前状态 |
|---|---|
| 基础工程 | ✅ FastAPI、Vue 3、依赖锁定、启动脚本已完成 |
| 数据库 | ✅ MySQL 8.4.11，33 张项目表，Alembic 位于 `b1f7c92de340 (head)` |
| 配置与密钥 | ✅ `.env` 隔离、高强度 JWT 密钥、运行配置校验 |
| AI | ✅ Qwen-Plus 严格 Schema、诊断与创意策划、Mock/DashScope 图片及视频异步生成已完成 |
| 认证 | ✅ 登录、当前用户、退出和令牌撤销完成 |
| 权限 | ✅ RBAC 权限点、路由权限和三角色基线完成 |
| 用户管理 | ✅ 列表、创建、更新、启停用、角色管理和最后管理员保护完成 |
| 前端业务 | ✅ 登录、应用壳、用户管理及 M2～M11 业务页面已完成 |
| 自动化验证 | ✅ 后端 58 项单元/集成/E2E 测试、Ruff、前端类型检查/构建及安全门禁通过 |
| 部署 | ✅ 本地脚本、Docker Compose、Nginx、CI、备份恢复、升级回滚和发布包完成 |

## 2. 任务状态

### M0：需求与工程基础

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M0-01 需求澄清和范围基线 | ✅ | Q-01～Q-12、MVP 边界、角色权限、口径和 v0.1 容量基线已固化 |
| M0-02 技术方案与架构设计 | ✅ | `技术方案与开发约定.md` |
| M0-03 工程脚手架与质量门禁 | ✅ | 锁文件、Ruff、Pytest、前端构建、依赖/安全扫描、GitHub Actions 和一键脚本完成 |
| M0-04 API 与错误规范 | ✅ | 统一错误、分页、Request ID、全量业务路由及 OpenAPI 已完成 |
| M0-05 数据库详细设计 | ✅ | 十二个迁移版本，模型与真实数据库无漂移 |
| M0-06 通用后端基础设施（新增） | ✅ | 日志、异常、请求上下文、分页、审计、配置校验完成 |

### M1：认证、用户与基础框架

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M1-01 后端认证 | ✅ | `/auth/login`、`/auth/me`、`/auth/logout`、JWT 撤销 |
| M1-02 RBAC 权限中间件 | ✅ | 12 个权限点、admin/operator/viewer 映射、403 标准错误 |
| M1-03 用户管理 | ✅ | `/users` 列表/创建、`PATCH /users/{id}`、审计和最后管理员保护 |
| M1-04 前端认证与应用壳 | ✅ | 登录、登录态恢复、路由守卫、权限菜单、退出和异常页完成 |
| M1-05 通用前端能力 | ✅ | 统一 API 客户端、通用表单/表格组件、分页和用户管理页完成 |

### M2～M12

| 模块 | 状态 | 下一步 |
|---|:---:|---|
| M2 店铺、商品、SKU 与库存 | ✅ | M2-01～M2-09 后端与前端纵向链路已完成 |
| M3 竞品与公开链接 | ✅ | 竞品、合规解析、监控快照和前端闭环已完成 |
| M4 AI 基础与诊断 | ✅ | Provider、五类 Prompt/Schema、诊断后端和前端闭环已完成 |
| M5 创意方案 | ✅ | 主图方案、视频脚本后端与前端闭环已完成 |
| M6 异步生成任务 | ✅ | 状态机、Worker、媒体适配器、任务操作和任务页闭环已完成 |
| M7 素材库 | ✅ | 文件转存、素材 API、审核版本和素材库页面已完成 |
| M8 推广链接、投放与实验 | ✅ | 链接、点击统计、投放建议、人工确认、实验状态机和前端闭环已完成 |
| M9 经营数据与复盘 | ✅ | 经营数据、指标汇总、复盘版本及下一轮诊断闭环已完成 |
| M10 导入、模板与演示数据 | ✅ | 三类模板、校验预览、部分成功确认、错误下载、导入中心及幂等演示数据完成 |
| M11 工作台与设置 | ✅ | 工作台聚合、首页、分组设置、密钥保护和生效提示已完成 |
| M12 测试、安全、文档与发布 | ✅ | M12-01～M12-06 全部完成，v0.1.0 发布包与验收证据就绪 |

### M2：店铺、商品、SKU 与库存

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M2-01 店铺后端 | ✅ | 分页筛选、创建、详情、更新、有效负责人及商品/低库存聚合 |
| M2-02 平台账号占位 | ✅ | 账号维护、脱敏响应、授权开始/回调/撤销及安全元数据白名单 |
| M2-03 商品与平台映射后端 | ✅ | 商品 CRUD/软删除、状态切换、筛选、聚合及平台映射完成 |
| M2-04 SKU 后端 | ✅ | 分页列表、创建、更新、启停、规格/金额校验及归属保护完成 |
| M2-05 库存与流水后端 | ✅ | 查询、配置、并发调整、流水分页和低库存查询完成 |
| M2-06 库存建议 | ✅ | 可解释规则生成、数据不足提示、快照和历史查询完成 |
| M2-07 店铺页面 | ✅ | 店铺列表/详情/编辑、库存概览、账号占位、库存建议与商品/SKU 追溯完成 |
| M2-08 商品列表与详情骨架 | ✅ | 商品列表/创建/编辑、九个 URL 标签页、只读权限和未保存离开提示完成 |
| M2-09 SKU 与库存前端 | ✅ | SKU 维护、库存配置/调整、流水分页、预警与版本冲突反馈完成 |

### M3：竞品与公开链接

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M3-01 竞品维护 | ✅ | 分页筛选、创建、详情、更新、软停用、商品归属和字段来源完成 |
| M3-02 公开链接解析任务 | ✅ | 白名单、安全限制、执行/重试、错误分类和选择字段回填完成 |
| M3-03 竞品监控与快照 | ✅ | 配置、原子领取、失败退避、到期执行、变化快照和人工采用完成 |
| M3-04 竞品前端 | ✅ | 竞品维护、解析进度/错误/重试、监控配置和快照差异页面完成 |

### M4：AI 基础与商品诊断

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M4-01 AI Provider 抽象 | ✅ | 统一文本接口、Mock/Qwen Provider、超时重试、限流、错误映射和脱敏用量日志完成 |
| M4-02 Prompt 与输出结构 | ✅ | 诊断、主图、视频、投放、复盘五类带版本 Prompt/Schema，包含裁剪、缺失提示、样例和注入防护 |
| M4-03 商品诊断后端 | ✅ | 商品/竞品/SKU 库存快照聚合、生成/列表/详情/编辑、原始输出留存和乐观锁完成 |
| M4-04 商品诊断前端 | ✅ | 生成确认、七段展示、失败/空状态、人工编辑、历史选择和查看人员只读完成 |

### M5：创意方案

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M5-01 主图方案后端 | ✅ | 每批至少三个完整方向、查询编辑、版本历史、唯一选中和归档规则完成 |
| M5-02 视频脚本后端 | ✅ | 每批至少三条完整脚本、分镜/口播/时长/转化引导编辑和历史追溯完成 |
| M5-03 主图方案前端 | ✅ | 方案卡片、结构化编辑、草稿/选中/归档、版本抽屉和 M6 生成入口状态完成 |
| M5-04 视频脚本前端 | ✅ | 脚本卡片、动态分镜编辑、状态操作、版本历史和 M6 生成入口状态完成 |

### M6：异步生成任务

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M6-01 任务状态机与仓储 | ✅ | 六状态转换、幂等创建、不可变方案快照、乐观版本、进度/错误和完整事件时间线完成 |
| M6-02 Worker 与队列 | ✅ | 数据库原子领取、心跳锁、并发 Worker、优雅停止、退避重试、超时/锁恢复和最终失败完成 |
| M6-03 图片/视频生成适配器 | ✅ | Mock/DashScope 统一接口、异步提交、外部任务轮询、结果校验和前端创建入口完成 |
| M6-04 重试、取消和超时 | ✅ | 人工重试/取消、版本冲突、权限审计、尝试上限、取消竞争和手工超时扫描入口完成 |
| M6-05 任务页 | ✅ | 筛选、进度、自动刷新/退避、详情、错误、结果、事件时间线和合法操作入口完成 |

### M7：素材库

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M7-01 文件与素材落库 | ✅ | 统一本地存储接口、安全下载校验、Worker 成功前转存、幂等同步、鉴权读取和失效检测完成 |
| M7-02 素材审核与版本 | ✅ | 连续素材版本、独立乐观锁、审核状态、评分、场景、标签、备注、操作人和审计完成 |
| M7-03 素材库前端 | ✅ | 图片/视频网格与预览、三类筛选、详情编辑、文件检查、审核操作及 viewer 只读完成 |

### M8：推广链接、投放建议与实验

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M8-01 推广链接建议与创建 | ✅ | 三类场景建议、安全目标 URL、UTM 白名单、随机追踪码、列表/详情/编辑/停用和乐观锁完成 |
| M8-02 跳转与点击统计 | ✅ | 公共跳转、事务计数、IP 摘要、机器人/10 分钟重复点击过滤、30 天统计和停用拦截完成 |
| M8-03 投放建议生成 | ✅ | 已审核素材/有效链接归属校验、八类严格结构化输出、待确认编辑、输入快照及 Provider/Prompt 追踪完成 |
| M8-04 投放建议人工确认 | ✅ | 确认/驳回权限、不可逆终态、备注、操作人、时间、并发版本和审计完成 |
| M8-05 投放实验计划 | ✅ | 从已确认建议生成、素材/链接校验、草稿编辑、预算校验、状态机、内容锁定和乐观锁完成 |
| M8-06 链接与投放前端 | ✅ | 链接建议/维护/点击统计、建议生成/确认/驳回、实验编辑/流转及 viewer 只读完成 |

### M9：经营数据与复盘

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M9-01 经营数据维护 | ✅ | 手工录入、查询、完整更新、软作废、周期防重叠、同商品关联校验和统一指标口径完成 |
| M9-02 复盘报告生成 | ✅ | 周期数据聚合、不可变输入快照、四段结构化报告、人工编辑和版本历史完成 |
| M9-03 复盘闭环 | ✅ | 从复盘生成下一轮诊断，来源报告与后续诊断双向可追溯 |
| M9-04 复盘前端 | ✅ | 经营指标/列表/表单、报告生成/编辑/版本及开启下一轮完成；经营数据文件导入已接入 M10 导入中心 |

### M10：导入中心、模板与演示数据

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M10-01 导入框架 | ✅ | UTF-8 CSV 上传、大小/行数/表头限制、逐行校验、只读预览、幂等确认、部分成功、批次查询和错误行下载完成 |
| M10-02 商品与 SKU 库存导入 | ✅ | 商品与 SKU/库存模板、同店商品防重、SKU 业务键更新、库存目标量校验及合规调整流水完成 |
| M10-03 经营数据导入 | ✅ | 日期/数字/指标/同商品关联校验、数据库与文件内周期防重叠、手工录入口径复用完成 |
| M10-04 导入中心前端 | ✅ | 模板下载、拖拽上传、字段说明、影响范围、逐行预览、错误下载、确认结果及批次历史完成 |
| M10-05 演示数据 | ✅ | 管理员幂等初始化、固定标识、完整业务链路、不同任务/素材/实验状态及安全基线校准入口完成 |

### M11：工作台与设置

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M11-01 工作台聚合接口 | ✅ | 店铺卡片、平台筛选、商品池、低库存、最近任务/复盘及指标聚合，使用有界聚合查询避免 N+1 |
| M11-02 首页工作台 | ✅ | 平台筛选、指标、店铺/商品入口、库存预警、任务/复盘直达和空环境演示引导完成 |
| M11-03 设置后端 | ✅ | 模型/系统/队列分组查询与更新、强校验、密钥加密只写、生效方式、变更审计及管理员权限完成 |
| M11-04 设置前端 | ✅ | 模型、用户、系统和任务配置导航；密钥不回显，来源和生效方式明确，非管理员无入口 |

### M12：测试、安全、文档与发布

| 任务 | 状态 | 说明/证据 |
|---|:---:|---|
| M12-01 自动化单元与集成测试 | ✅ | 58 项测试覆盖权限、事务、状态机、导入、AI Schema、安全失败分支，全部使用 Mock 外部服务 |
| M12-02 端到端测试 | ✅ | 三角色登录/权限及完整单品演示链路 E2E 在空测试库稳定通过 |
| M12-03 安全与合规检查 | ✅ | 鉴权、越权、输入、SSRF、文件、敏感值、pip/npm 依赖与 Bandit 高危门禁通过 |
| M12-04 性能与可靠性验证 | ✅ | 固定工作台 SQL 数、1000 行导入、点击去重、库存冲突、任务锁/超时/幂等验证通过 |
| M12-05 文档与演示脚本 | ✅ | README、部署/升级/回滚/备份、配置、API、模板、管理员手册和 8 分钟演示讲稿完成 |
| M12-06 发布与验收 | ✅ | v0.1.0 ZIP/SHA-256、Docker Compose、CI、发布说明、已知问题、回滚和 10 条验收证据完成 |

## 3. 本轮完成记录

### 2026-09-18：M12-01～M12-06

- 新增发布级 API E2E，在全新测试库中登录管理员、运营人员和查看人员，幂等初始化演示数据，遍历店铺到下一轮诊断的完整单品闭环。
- 新增发布质量用例：工作台查询保持固定 13 条 SQL（含 2 条鉴权）；1000 行 CSV 预览通过，1001 行提前拒绝；受保护 API、角色边界、SQL 注入输入和 Request ID 注入均获得稳定结果。
- 后端全量测试增至 58 项，覆盖主成功路径与权限、校验、并发冲突、上游失败、超时和幂等分支；不调用付费模型。
- 新增仓库密钥特征扫描和可重复执行的质量门禁脚本；Ruff、pip-audit、npm audit 生产依赖与 Bandit 高危扫描通过，未发现高危未处理问题。
- 固化 Q-01～Q-12 最终技术/业务结论和 v0.1 容量基线，明确了单工作区、分页、导入、Worker、库存与点击可靠性边界。
- 新增后端镜像、前端 Nginx 镜像和 MySQL/API/Worker/Web Docker Compose；API 使用 8011、Web 默认 8080，不暴露 MySQL 主机端口且不使用 8000。
- 新增 GitHub Actions CI，在 MySQL 8.4 服务上执行迁移、漂移检查、后端测试/规范、前端类型/构建以及安全/依赖门禁，失败时上传诊断报告。
- 新增部署、升级、数据库/素材备份恢复与回滚文档；补齐管理员手册、导入说明、8 分钟演示讲稿、测试/安全/性能报告、发布说明和验收记录。
- 生成 v0.1.0 发布 ZIP 及 SHA-256 校验文件；原需求 10 条验收标准全部通过，P0 未关闭缺陷为 0，无书面豁免。
- M12 无数据库结构变更；真实 MySQL 仍为 `b1f7c92de340 (head)` 且 Alembic 无漂移。

### 2026-09-18：M11-01～M11-04

- 新增工作台聚合接口，一次返回可用平台、全局指标、店铺卡片、商品池、低库存、最近生成任务和运营复盘；平台筛选在各聚合口径中一致生效。
- 聚合服务使用子查询、分组统计和有界列表查询，未在结果循环内追加 SQL；已在真实 MySQL 上执行聚合并与明细口径核对。
- 首页升级为运营工作台，支持平台筛选、店铺/商品跳转、低库存直达、任务与复盘入口；写操作和演示数据初始化按权限显示。
- 空环境不再只显示空表：运营人员可创建店铺/商品，管理员可幂等初始化 M10 演示数据，其他角色获得明确引导。
- 新增模型、系统和队列三组设置 API，仅管理员可访问；每个设置项声明类型、边界、可选值、配置来源和生效方式。
- 模型 API Key 使用基于服务端 JWT 密钥派生的 Fernet 密钥加密保存，查询仅返回“已配置”状态；审计日志只记录变更键名而不记录新旧密钥。
- 文本模型与竞品解析配置对新请求生效；新建任务立即使用默认媒体参数和尝试上限；媒体 Provider、存储下载和 Worker 队列参数在重启 Worker 后生效。
- 系统设置页展示环境/数据库来源和生效标记，敏感输入留空表示保持原值；用户配置标签直达现有用户管理页。
- M11 复用现有 `system_settings` 表，无需新迁移；真实 MySQL 仍为 `b1f7c92de340 (head)` 且模型无结构漂移。
- 后端全量 54 项测试、Ruff、前端严格类型检查和生产构建通过。

### 2026-09-18：M10-01～M10-05

- 新增统一 CSV 导入框架，首版限定 UTF-8、2MB 和 1000 行；上传后只保存批次及逐行校验结果，不写商品、库存或经营数据。
- 导入模板采用稳定英文表头并附示例行与中文字段说明；支持商品、SKU 与库存、经营数据三类模板直接下载。
- 每行保存原始值、标准化值、列级错误码/原因和最终目标 ID；错误行可下载为带错误列、错误码和原因的修正 CSV。
- 确认导入以批次为单位加锁并可重复安全调用，每个有效行使用独立保存点，支持部分成功且始终满足“成功数 + 失败数 = 总行数”。
- 商品导入校验有效店铺、金额、URL、状态和同店同名重复；SKU 库存按“商品 ID + SKU 编码”创建或更新，目标库存变化生成带批次引用的正式库存流水。
- 经营数据导入复用 M9 的日期、非负指标、`转化数 ≤ 点击数 ≤ 曝光数`、同商品引用和周期不重叠规则，并额外拒绝同一文件内的交叉周期。
- 新增独立导入中心页面，支持类型切换、模板下载、拖拽文件、字段说明、逐行预览、确认前影响范围、部分成功结果和历史批次回看；M9 经营数据入口已直接接入。
- 新增仅管理员可用的演示数据初始化入口，以固定店铺标识保证幂等；一次调用生成店铺、商品、SKU/库存、竞品、诊断、方案、生成任务、素材、链接、建议、实验、经营数据、复盘和下一轮诊断。
- 演示数据包含成功/失败任务、通过/驳回素材、已完成/草稿实验等状态；素材文件使用本地 Mock 数据，不调用付费模型或真实平台。
- M10 复用初始迁移中既有的 `import_batches`、`import_rows`，无需新增数据库表；真实 MySQL 仍位于 `b1f7c92de340 (head)`，共 33 张项目表。
- 后端全量 52 项测试、Ruff、前端严格类型检查和生产构建通过。

### 2026-09-18：M9-01～M9-04

- 新增经营数据创建、分页查询、详情、完整更新和软作废接口；作废记录保留操作人、时间和版本，不再参与汇总或复盘。
- 有效记录按商品禁止经营周期重叠；曝光、点击、转化、花费和收入必须非负，并强制满足“转化数 ≤ 点击数 ≤ 曝光数”。
- 创意方案、生成素材、推广链接和投放实验均校验属于同一商品；写操作使用版本号拒绝并发覆盖。
- 指标统一为 `CTR=点击/曝光`、`转化率=转化/点击`、`ROI=收入/花费`，保留 6 位小数，分母为零时返回空值。
- 复盘生成只聚合完整落入所选周期的有效记录，冻结逐条记录与汇总快照；无数据时返回明确业务错误，不创建空报告。
- 报告保存周期摘要、核心洞察、问题判断、下一步动作、原始模型输出、Provider、模型和 Prompt/Schema 版本；人工编辑会新增不可变内容版本。
- 可从任一复盘报告直接生成下一轮诊断；诊断保存来源报告 ID，报告详情同时返回全部后续诊断，实现双向追溯。
- 商品“复盘”标签已升级为经营数据与运营复盘工作台，支持指标总览、记录维护/作废、报告生成/编辑、版本时间线和开启下一轮；查看人员保持只读。
- M9 当时预留的经营数据导入入口现已接入 M10 导入中心，支持文件上传、校验预览、确认导入和错误行下载。
- 新增迁移 `b1f7c92de340_m9_performance_reviews.py` 并应用到真实 MySQL，数据库共 33 张项目表且 Alembic 无结构漂移。
- 后端全量 49 项测试、Ruff、前端严格类型检查和生产构建通过。

### 2026-09-18：M8-01～M8-06

- 新增推广链接场景建议、分页列表、详情、创建、编辑、启停和统计 API；目标地址只接受不含认证信息的公开 HTTP(S) URL，拒绝 localhost、内网和非公网 IP。
- 追踪码使用安全随机数生成；UTM 只允许标准字段并保留目标地址原有查询参数，停用链接统一拒绝跳转。
- 点击记录与累计计数处于同一事务；客户端 IP 仅保存基于服务端密钥的不可逆 HMAC 摘要，不保存原始 IP。
- 疑似机器人和同一链接 10 分钟内的重复访问会完整留痕并标注过滤原因，但不进入有效点击；统计支持有效点击、过滤点击、独立访客和按日明细。
- 投放建议生成强制选择本商品已审核且文件可用的素材与有效推广链接，保存商品、最新诊断、素材、链接和操作说明快照。
- 建议保存八类结构化内容、原始输出、Provider、模型、Prompt/Schema 版本和 AI 用量；待确认内容可并发安全地人工编辑，失败不创建伪建议。
- 人工确认/驳回只允许具备 `ad.confirm` 权限的角色操作，终态不可撤回，记录备注、操作人、时间和审计日志；系统不会调用真实广告平台。
- 已确认建议可生成实验草稿；素材和链接再次校验商品归属及可用状态，预算必须非负，确认后内容锁定。
- 实验严格执行 `draft → confirmed → running → finished`，非终态可转为 `cancelled`；非法流转和并发覆盖返回冲突。
- 商品“链接”和“投放”标签已替换为完整工作区，包含链接复制/统计、建议历史与八段内容、二次确认、实验编辑和合法状态操作；viewer 只读。
- 新增迁移 `a8d34f91c2e0_m8_marketing_tracking.py` 并应用到真实 MySQL，Alembic 当前为 head。
- 后端全量 47 项测试、Ruff、前端严格类型检查和生产构建通过。

### 2026-09-17：M7-01～M7-03

- 新增统一文件存储接口和本地原子写入实现，存储 Key 被限制在配置根目录内，阻止绝对路径与目录穿越。
- 真实生成结果下载仅允许标准 HTTPS，逐跳检查重定向和公网地址，限制超时与图片/视频大小，并同时校验声明 MIME 与 PNG/JPEG/WebP/MP4/WebM 文件签名，拒绝可执行或类型不匹配内容。
- Worker 在外部生成成功后先完成文件转存和素材落库，再将任务置为成功；临时下载/存储故障保留外部任务号继续重试，非法结果进入明确失败状态。
- Mock 图片/视频任务生成本地可验证占位文件，自动化测试和本地演示不依赖临时外部域名。
- 使用“生成任务 + 结果序号”唯一键保证重复同步不新增素材；同方案同类型素材在方案行锁下生成连续版本，存储 Key 另有完整唯一索引。
- 素材保存 MIME、大小、SHA-256、图片宽高/视频时长、模型、来源任务与方案、同步时间；内容读取需要登录，并在文件缺失或哈希不一致时标记 `missing/invalid`。
- 新增素材列表、详情、鉴权内容读取、元数据更新、审核、文件检查和历史成功任务补同步 API。
- 场景、评分、标签、备注和审核操作使用独立 `lock_version` 防止并发覆盖；审核记录操作人/时间和审计日志，损坏或缺失文件不能通过审核。
- 商品“素材库”标签新增图片/视频卡片网格、安全 Blob 预览、媒体/审核/文件状态筛选、版本来源、评分标签、元数据编辑、文件检查和审核；viewer 保持只读。
- 新增迁移 `f69c2a8e14b7_asset_storage_and_review.py` 并应用到真实 MySQL；数据库仍为 32 张项目表，Alembic 模型无结构漂移。
- 45 项后端测试、Ruff、前端类型检查和生产构建通过。

### 2026-09-17：M6-04～M6-05

- 新增任务取消与人工重试 API，统一要求 `job.operate` 权限和 `expected_version` 乐观锁，状态竞争时拒绝静默覆盖。
- 取消仅允许 `pending/running`；成功、失败、已取消和超时任务不能取消。运行中取消会立即阻止 Worker 在本地落库后续结果，并明确提示供应商侧任务可能继续执行。
- 人工重试仅允许 `failed/timeout` 且未达到最大尝试次数；使用原不可变输入快照重新排队，失败任务清理旧外部编号，已有外部任务号的超时任务恢复轮询以避免重复提交。
- 取消与重试均保存操作人、原因、状态事件和审计日志；新增 `--scan-only` 运维入口，可单独执行超时任务及过期锁扫描。
- 商品“生成任务”标签新增任务列表、图片/视频类型筛选、六状态筛选、进度、尝试次数和更新时间；从方案创建任务后会直接进入对应任务详情。
- 任务详情展示精确方案版本、不可变参数、Provider、外部任务号、错误码、临时生成结果和完整事件时间线。
- 活跃任务每 5 秒自动刷新，接口失败时指数退避至 30 秒；任务全部进入终态、页面隐藏或组件离开后停止轮询。
- 重试和取消按钮仅对有 `job.operate` 权限且处于合法状态的用户显示；达到尝试上限时只显示明确提示。
- 后端 43 项测试、Ruff、前端类型检查和生产构建通过；本批无数据库结构变更。

### 2026-09-17：M6-01～M6-03

- 新增生成任务创建、分页、详情和事件接口；只允许从已选中且未归档的主图/视频方案创建，并锁定精确 `creative_plan_id + version_no`。
- 同一幂等键只创建一条任务；幂等键复用到不同商品或方案时返回冲突，创建请求只入队并立即返回，不等待外部生成完成。
- 任务输入保存方案内容、版本、Prompt/Schema 版本、模型、尺寸、视频时长、轮询策略和请求时间，避免后续编辑导致输入漂移。
- 建立 `pending/running/succeeded/failed/cancelled/timeout` 六状态状态机、非法流转保护、进度、稳定错误码、尝试次数和乐观版本；每次状态变化均写入事件。
- 新增独立 Worker 进程和启动脚本，使用数据库行锁原子领取，支持多线程并发、心跳、锁过期恢复、任务超时扫描、指数退避、最大尝试次数和优雅停止。
- Worker 将外部 PENDING/RUNNING 映射为本地运行态和下次轮询时间；成功结果仅落库一次，外部任务已创建后的临时轮询故障不会重复提交。
- 新增统一 `MediaAdapter` 协议、确定性 Mock 实现和 DashScope 实现；图片、视频均采用异步提交后轮询，校验外部任务编号、HTTPS 结果地址、图片尺寸和视频时长。
- 主图和视频卡片的生成入口已启用；用户确认后携带方案精确版本和客户端幂等键创建任务，成功后显示任务编号。
- 新增迁移 `e53f09c42a17_generation_job_state_machine.py` 并应用到真实 MySQL；数据库仍为 32 张项目表，Alembic 模型无结构漂移。
- 42 项后端测试、Ruff 和前端生产构建通过；真实媒体接口未产生付费调用，DashScope 请求协议通过模拟响应覆盖。

### 2026-09-17：M5-01～M5-04

- 新增主图方案与视频脚本统一接口：从最新或指定商品诊断聚合输入快照，每次有效生成分别至少三个完整方向或三条完整脚本。
- 主图方向完整保存标题、视觉概念、构图、画面文案、生图提示词和理由；视频脚本完整保存钩子、有序分镜、画面、口播、时长、转化引导和理由。
- 新增生成批次、原始输出、输入快照、Provider/模型/Prompt/Schema、生成与编辑人等追溯字段。
- 新增 `creative_plan_revisions` 不可变版本表；生成、内容编辑和状态变化均形成版本快照。
- 建立草稿、选中、归档状态机：同商品同类型最多一个选中方案；新选中会让原方案回到草稿；选中方案需先取消选中才能归档；归档后不可编辑或恢复。
- 内容编辑和状态操作均校验 `expected_version`，冲突时拒绝静默覆盖；跨商品访问返回资源不存在。
- 商品详情“主图”标签新增方向卡片、结构化编辑、选中/取消/归档、版本历史和状态筛选。
- 商品详情“视频”标签新增脚本卡片、动态分镜编辑、口播/时长/转化引导维护、状态操作与版本历史。
- M6 尚未实现前，只有选中方案展示媒体生成入口且保持禁用，并明确会在 M6 携带正确方案 ID 与版本号发起异步任务；归档方案无生成入口。
- 真实 `qwen-max` 的 JSON Object 模式无法稳定保持复杂视频嵌套结构，因此未放宽 Schema；改用独立的 `qwen-plus` 严格 JSON Schema 模型。真实验证中主图和视频均一次通过，分别返回三个方向、三条脚本且每条脚本三个分镜。
- 新增迁移 `c42b18d7319a_creative_plan_versions.py` 并应用到真实 MySQL，数据库共 32 张项目表且 Alembic 无结构漂移。
- 36 项后端测试、Ruff 和前端生产构建通过。

### 2026-09-17：M4-01～M4-04

- 新增统一文本生成 Provider 协议和确定性 Mock Provider；未配置密钥时自动使用 Mock，真实环境使用 Qwen OpenAI 兼容接口。
- Qwen Provider 支持请求超时、网络和上游繁忙重试、进程内每分钟限流、稳定错误码、Token/耗时/尝试次数统计，以及结构非法后的纠正重试。
- 新增诊断、主图方案、视频脚本、投放建议和复盘五类严格 JSON Schema 与版本化 Prompt；统一执行输入裁剪、缺失数据声明、合法示例和 Prompt 注入隔离。
- 商品诊断聚合商品、启用竞品、SKU/库存和可选来源复盘，成功后保存输入快照、原始输出、模型、Provider、Prompt/Schema 版本和七项必需结论。
- 生成失败或输出结构非法时不创建诊断记录，仅保存不含密钥和业务正文的失败用量日志。
- 新增诊断历史、详情和编辑接口；编辑使用 `expected_version` 乐观锁，避免多人操作静默覆盖。
- 商品详情“诊断”标签新增生成确认、数据缺失提示、七段结构化展示、编辑保存、历史记录选择及加载/失败/空状态；viewer 保持只读。
- 新增迁移 `a61d9e083b42_ai_provider_and_diagnosis.py` 并应用到真实 MySQL，数据库共 31 张项目表且 Alembic 无结构漂移。
- 31 项后端测试、Ruff 和前端生产构建通过；真实 `qwen-max` 在一次请求内返回并通过七字段 Schema 校验，API Key 未输出。

### 2026-09-17：M3-01～M3-04

- 实现商品下竞品分页、平台/状态/关键词筛选、创建、详情、更新和软停用，跨商品访问返回资源不存在。
- 竞品公开字段覆盖名称、平台、链接、价格、销量提示、标题、主图、卖点和评价关键词，并逐字段记录手工、解析或监控来源。
- 实现公开链接解析任务的创建、列表、详情、执行和最多三次重试；成功结果不会自动覆盖竞品，必须人工勾选字段后回填。
- 解析器仅允许配置白名单内的 HTTP(S) 域名和标准端口，逐跳校验重定向与域名解析结果，并限制超时、响应大小和重定向次数。
- 登录、验证码、访问拒绝、非 HTML、超大响应、网络超时和无公开元数据均形成明确错误码，不实现绕过平台控制的逻辑。
- 实现竞品监控启停与频率配置、立即执行和到期批量入口；使用领取锁避免同一监控并发重复运行。
- 监控失败采用指数退避，连续三次失败转为失败状态；每次成功或失败均保存快照，成功快照记录相对上次的变化字段。
- 监控快照不会自动覆盖手工字段，用户可选择采用变化值并保留 `monitor` 字段来源。
- 商品详情“商品&竞品”标签新增竞品维护、解析任务状态与错误、重试、逐字段回填、监控配置、快照对比和人工采用界面。
- 新增迁移 `f318c24e8a71_competitor_operations.py` 并应用到真实 MySQL；28 项后端测试、Ruff、前端类型检查通过，Alembic 无结构漂移。

### 2026-09-17：M2-07～M2-09

- 新增店铺列表和详情页，支持平台/状态/关键词筛选、创建编辑、经营聚合、店铺商品和库存概览。
- 店铺低库存、库存列表和库存建议均可直接追溯到具体商品与 SKU；平台账号提供脱敏展示和授权占位操作。
- 新增商品列表和创建入口，展示店铺、平台、分类、价格、状态、平台映射、最近诊断和最近复盘。
- 建立商品九标签工作区，各标签拥有可直接访问的 URL 并始终以路由商品编号加载上下文；无效标签自动回到“商品&竞品”。
- “商品&竞品”标签已接入商品编辑、平台映射维护、SKU 管理与库存能力，其余标签提供后续里程碑可直接扩展的结构化骨架。
- 新增未保存离开提醒，浏览器刷新/关闭和跨页面离开均会提示；查看人员仅展示只读入口。
- 实现 SKU 分页筛选、新增编辑、启停、规格维护、库存预警阈值和库位配置。
- 实现入库、出库、盘点、锁定和解锁操作；提交携带库存版本号，成功后即时刷新，冲突或失败只显示后端真实结果。
- 实现 SKU 库存流水抽屉、类型筛选和分页，并从店铺预警与建议入口定位高亮目标 SKU。
- 前端类型检查和生产构建通过；真实 MySQL 就绪检查和认证后店铺/商品/库存列表接口烟雾测试通过。

### 2026-09-16：M2-06

- 实现店铺维度库存建议生成、最新建议、历史批次分页和指定批次详情接口。
- 首版使用确定性规则引擎：默认汇总近 30 天出库，计算 14 天覆盖量并应用 1.2 安全系数。
- 建议逐条定位商品与 SKU，保存库存、锁定量、预警阈值、出库量、目标库存和建议补货量输入快照。
- 输出补货、监控、健康三类动作和四级优先级，每条建议均包含可核对的中文解释。
- 出库事件不足默认 2 次时标记数据不足，不伪造需求预测，并给出人工复核提示。
- 建议生成只读库存数据，不修改库存数量、版本号或库存流水；查看人员可读但不能生成。
- 新增 `inventory_advice_runs`、`inventory_advice_items` 两张追溯表和迁移 `e74ab89d13c2_add_inventory_advice_snapshots.py`。
- 真实 MySQL 已升级至新迁移；Ruff 通过，25 项后端自动化测试通过，Alembic 无结构漂移。

### 2026-09-16：M2-05

- 新增 SKU 时自动创建零库存记录，并通过数据迁移为历史 SKU 补齐缺失库存。
- 实现全局库存分页、店铺/商品/SKU 状态/关键词筛选，以及有效商品低库存分页查询。
- 实现 SKU 库存详情、预警阈值和库位配置；配置更新使用期望版本防止覆盖。
- 实现入库、出库、盘点调整、锁定和解锁五类库存变更，并记录调整前后数量、原因、引用来源和操作人。
- 库存调整同时使用数据库行锁和 `expected_version` 乐观锁；版本冲突返回当前版本且不产生流水。
- 明确负库存规则：总库存、锁定量不得为负，锁定量不得超过总库存；对应约束同时下沉至数据库。
- 停用商品、SKU 或店铺不能增加/锁定库存，但允许出库和解锁完成存量清理。
- 新增迁移 `d91e6f7a24b0_backfill_inventory_items.py` 并应用至真实 MySQL。
- Ruff 检查通过，23 项后端自动化测试通过，Alembic 模型结构检查无差异。

### 2026-09-16：M2-04

- 实现商品下 SKU 分页列表、状态/关键词筛选、创建、详情、更新和启停接口。
- SKU 编码在同一商品内按不区分大小写唯一，并由数据库唯一键兜底并发冲突。
- SKU 编码限制为字母、数字、点、下划线和连字符；名称和平台 SKU 编号统一去除首尾空白。
- 规格 JSON 统一为最多 20 项的扁平字符串键值对，限制规格名称和规格值长度。
- SKU 价格和成本使用两位小数 Decimal 且必须非负，成本和平台 SKU 编号支持清空。
- 所有详情和写操作同时校验 SKU 属于 URL 指定商品，跨商品操作返回资源不存在。
- 停用商品或店铺不能新增/启用 SKU；SKU 停用不删除记录、库存或历史流水。
- Ruff 检查通过，21 项后端自动化测试通过；本任务无数据库结构变更。

### 2026-09-16：M2-03

- 实现商品分页列表、店铺/平台/状态/分类/关键词筛选、创建、详情和更新接口。
- 商品平台从所属店铺自动继承，商品创建后不可跨店铺迁移；停用店铺不能创建或启用商品。
- 价格和成本使用两位小数 Decimal 且必须非负；商品链接和图片地址仅允许合法 HTTP(S) URL。
- 实现商品启停状态接口；删除使用 `inactive` 软删除，避免级联清除诊断、素材等下游数据。
- 商品列表和详情聚合平台映射数、最近诊断时间和最近复盘时间。
- 实现平台商品映射列表、创建、更新和删除接口，所有写操作校验映射属于 URL 指定商品。
- 映射唯一键调整为“店铺 + 平台 + 平台商品编号 + 平台 SKU 编号”，同店铺禁止跨商品重复绑定，不同店铺可复用平台编号。
- 新增迁移 `c30f18a973de_scope_product_mappings_to_store.py` 并应用至真实 MySQL。
- Ruff 检查通过，18 项后端自动化测试通过，Alembic 模型结构检查无差异。

### 2026-09-16：M2-01～M2-02

- 实现店铺分页列表、平台/状态/负责人/关键词筛选、创建、详情和更新接口。
- 平台枚举覆盖淘宝、天猫、京东、拼多多、抖音、快手、小红书、微信及其他平台。
- 店铺负责人改为关联有效应用用户；保留旧负责人文本字段用于兼容历史数据。
- 店铺列表和详情返回商品数、低库存数，独立聚合接口同时返回 SKU 和授权账号统计。
- 同平台外部店铺编号增加数据库唯一约束；已有账号或商品时禁止变更店铺平台。
- 实现平台账号创建、查询、更新、授权开始、授权回调占位和授权撤销接口。
- 账号标识只以脱敏形式返回；授权 `state` 只保存 SHA-256 摘要并禁止重放。
- `auth_meta_json` 仅允许卖家 ID、权限范围、过期时间、授权时间和错误码等安全字段；请求中的额外密码、Token、Cookie 字段会被拒绝。
- 新增迁移 `b82c44df8f14_add_store_owner_and_external_id_key.py` 并应用至真实 MySQL。
- Ruff 检查通过，15 项后端自动化测试通过，Alembic 模型结构检查无差异。

### 2026-09-16：M1-04～M1-05

- 实现登录页、Token 持久化、刷新后用户恢复、主动退出和 401 统一回登录流程。
- 实现登录/访客/权限路由守卫，以及 403、404 页面。
- 实现响应式应用壳、可折叠侧栏和按角色/权限显示的导航菜单。
- 新增统一 Axios 客户端，统一处理 Bearer Token、后端标准错误和未授权事件。
- 新增通用页面标题、数据表格容器和表单弹窗组件。
- 实现用户管理页面：筛选、分页、创建、编辑、启停用、角色调整和密码强度提示。
- 实现工作台健康状态展示和后续模块入口占位。
- Element Plus 调整为按需加载，页面路由采用懒加载；生产构建无大包告警。
- 前端类型检查、生产构建通过；通过 Vite 代理验证登录页、健康检查、未登录 401 和 `X-Request-ID`。

### 2026-09-16：通用基础设施与 M1-01～M1-03

- 新增统一错误格式：`code`、`message`、`request_id`、`details`。
- 新增 `X-Request-ID` 透传/生成和 JSON 结构化访问日志。
- 新增分页响应、审计日志辅助服务和启动配置校验。
- JWT 增加 `jti`，新增 `auth_token_revocations` 实现退出后令牌失效。
- 实现登录、当前用户和退出接口。
- 实现 12 个权限点和三种角色映射。
- 实现用户列表、创建和更新；禁止停用或降级最后一个有效管理员。
- 密码使用 Argon2，创建/修改密码执行强度校验。
- 新增迁移 `7ebf4f3fc50e_add_auth_token_revocations.py` 并应用至真实 MySQL。
- 后端静态检查通过，12 项自动化测试通过，迁移结构检查无差异。

## 4. 下一批建议任务

M1～M7 已完成。建议下一次进入：

`M8-01～M8-06：推广链接、点击统计、投放建议、人工确认、实验计划及对应前端。`

M8 应只引用同商品且已审核通过的素材，不执行任何真实广告投放；高风险确认操作必须保留权限和审计记录。

## 5. 验收与安全提醒

- 正式库目前没有自动创建默认应用管理员；使用 `scripts/create-admin.ps1` 交互创建。
- `.env` 和所有密钥不进入版本库；日志不记录请求体、密码或 Token。
- 每批任务完成后更新本文件，并记录测试、迁移和遗留问题。
- 数据库字段变更统一新增 Alembic 迁移，不直接手工改表。
